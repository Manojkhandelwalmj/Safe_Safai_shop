
/// List of Enums
/// They can't be created inside a class
library;

enum TextSizes {small, medium, large}

enum OrderStatus {processing, shipped, delivered}

enum PaymentMethods {paypal, googlePay, paytm, phonePe, visa, applePay, masterCard, creditCard, paystack, razorPay}